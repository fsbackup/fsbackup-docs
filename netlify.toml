[build]
  command = "git submodule update --init --recursive && npm ci --legacy-peer-deps && npm run generate"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--legacy-peer-deps"
